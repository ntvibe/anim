name: Anim feature syntax check
on:
  push:
    branches: [keyframes-nulls-curve-library]
  workflow_dispatch:
jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check latest JavaScript syntax
        run: node --check experiments/01-bezier-logo-text/app.js
